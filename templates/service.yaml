apiVersion: v1
kind: Service
metadata:
  name: {{ include "integrador-chart.fullname" . }}
  labels:
    {{- include "integrador-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "integrador-chart.selectorLabels" . | nindent 4 }}
